# 共有カレンダーアプリ 開発ガイドライン

チーム開発(5人)を進めるにあたって決めた内容をまとめたもの。着手前に全員で目を通しておくこと。

---

## 1. 企画概要

- TimeTreeを参考に、「一括予定登録」と「空き日程の自動ソート表示」を軸にした共有カレンダーアプリ
- ターゲットユーザーは友人・サークル仲間との遊びの予定調整
- 開発期間は1〜2ヶ月を想定。目的は**期限内に確実に完成させること**

## 2. 技術スタック

- Spring Boot + MyBatis + MySQL(`campustrade`と同じ構成)
- フロントエンドはThymeleaf継続、一部画面のみJavaScript + fetch APIで非同期更新
- **Google Calendar APIは使わない。** カレンダー・予定・共有・コメント・通知はすべて自前DB(MySQL)で管理する
  - 理由:OAuth認証まわりの複雑さ(審査・トークン期限)に対して得られるものが少ない
  - Google連携は「確定予定を自分のGoogleカレンダーに反映する」任意のオプション機能としてのみ、時間が余れば後付けする

## 3. DB設計

11テーブル構成(詳細は別添の `schema.sql` を参照)。

| テーブル | 役割 |
|---|---|
| users | ユーザー情報 |
| otp_codes | ログイン時の2段階認証コード(メールOTP) |
| calendars | カレンダー(個人用・共有用を`is_personal`で区別。「学年予定」なども名前を変えて作るだけで対応可) |
| calendar_members | カレンダーとユーザーの多対多、役割(owner/member)と状態(pending/accepted)を持つ |
| invite_codes | 招待コード発行・有効期限管理 |
| schedules | 確定済みの予定本体(`location`列あり)。一括登録は日程ごとに独立したレコードとして保存し、行ごとに別タイトルも可 |
| schedule_polls | 空き日程調整(調整さん型)の候補セット。確定すると`schedules`に1件作られる |
| poll_candidates | 調整さんの候補日程(開始・終了時刻) |
| poll_responses | 候補日程ごとのメンバーの○×回答 |
| comments | 予定へのコメント |
| notifications | 通知(予定削除時も`schedule_id`はNULLにして履歴は残す) |

決定事項:
- カレンダー削除時は中身(メンバー・予定・招待コード)も連鎖削除(`ON DELETE CASCADE`)
- 通知だけ例外的に`ON DELETE SET NULL`(元の予定が消えても通知文言は残す)
- 予定は「日程ごとに独立したレコード」方式(案A)で統一。コメントも日程ごとに別スレッドになる
- 空き日程は自動計算型ではなく**調整さん型**(候補提示→○×回答→確定)を採用

## 4. 画面一覧(10画面)

1. ログイン/新規登録
2. 認証コード入力(2段階認証)
3. カレンダー一覧
4. カレンダー詳細(月表示。週表示・日表示は時間が余れば追加)
5. 予定登録/一括登録
6. 予定詳細/コメント
7. 空き日程を探す・回答(候補への○×回答と、幹事による確定を1画面で兼ねる想定 ※要チーム確認)
8. カレンダー作成/招待
9. 招待コード入力
10. 通知一覧

ワイヤーフレームは別添のHTMLファイル一式を参照(2段階認証・空き日程調整画面は追加分、別途作成予定)。

## 5. 担当分担(5人)

機能ドメイン単位で分担する(画面単位で分けない理由は8-1参照)。依存関係が近いものはまとめて1人に割り振ってある。

| 担当 | ドメイン | 担当する画面 | 負荷感 | 追加役割 |
|---|---|---|---|---|
| A | 認証・ユーザー管理 | ①ログイン/新規登録 | 軽 | — |
| B | カレンダー管理 | ②カレンダー一覧 / ③カレンダー詳細(月表示) | 中(月表示のグリッド描画がやや手間) | 共通レイアウト(`layout.html`・`common.css`)の作成・管理 |
| C | 予定登録 | ④予定登録/一括登録 / ⑤予定詳細(本体部分) | 中 | — |
| D | 空き日程 | ⑥空き日程表示 | 重(区間マージのアルゴリズム) | — |
| E | コメント・通知・招待共有 | ⑤予定詳細(コメント欄) / ⑨通知一覧 / ⑦カレンダー作成・招待 / ⑧招待コード入力 | 軽〜中(画面数は多いが個々は単純なCRUD) | 全画面の見た目の最終統一チェック |

依存関係:D・EはCの`schedules`が固まらないと着手しづらいため、Cは`schedules`のCRUDを優先して早めに共有する。

画面数だけ見るとEが4画面で多く見えるが、招待コード発行・照合は複雑な計算のない単純なCRUDで、Dの1画面(空き日程のアルゴリズム)より実装コストは軽い。着手前にこの前提をチームに共有しておくこと。

## 6. 開発の進め方(順序)

機能単位で縦に貫通させて進める(全画面のUIを先に作ってから繋ぐ、はNG)。

1. DDLを書き起こす(完了、`schema.sql`参照)
2. 開発環境とプロジェクト雛形を作る
3. 認証機能を実装する
4. カレンダー一覧・詳細(月表示)を実装する
5. 予定登録(一括登録)を実装する ← ここまでで最初のデモ可能なマイルストーン
6. 空き日程表示を実装する(ロジックはUIと切り離してテストコードで先に固める)
7. コメント・通知・招待機能を実装する
8. テスト・デモ用データ整備と発表準備(開発最終週は新機能を足さず、この作業に専念する)

## 7. チーム共通ルール(着手前に全員で決めること)

**(1) ログイン中ユーザーIDの取得方法**
共通の`LoginUser`クラス(id・name・emailを持つ)を用意し、Controllerでは`@AuthenticationPrincipal LoginUser loginUser`で統一して受け取る。Aが最初に用意する。

**(2) 権限判定の置き場所**
`CalendarAuthService`に集約する(`isOwner(userId, calendarId)` / `isMember(userId, calendarId)`)。各自が独自に権限判定を書くのは禁止。Bが最初に用意する。

**(3) 日時の受け渡し方式**
- フォーム:`<input type="date">`+`<input type="time">`を別々に受け取り、Controller側で`LocalDateTime`に結合
- fetch APIで送る場合:ISO8601形式(`2026-09-20T19:00`)に統一
- MyBatisの型ハンドラは標準の`LocalDateTimeTypeHandler`で足りる

**(4) URL設計とパッケージ構成**
```
/auth/login, /auth/signup                    → A
/calendars, /calendars/{id}                  → B
/calendars/{id}/invites, /invites/{code}/join → B
/calendars/{id}/schedules, /schedules/{id}   → C
/calendars/{id}/free-slots                   → D
/schedules/{id}/comments, /notifications     → E
```
パッケージは `controller` / `service` / `mapper` / `domain` / `dto` で分割。`LoginUser`と`CalendarAuthService`は`common`パッケージに置く。

**(5) 共通レイアウト**
Bが `fragments/layout.html`(共通ヘッダー・ナビ)と `common.css`(ワイヤーフレームで使った色のCSS変数)を最初に用意し、各画面は`th:replace`で読み込む。

**(6) エラー表示**
- 入力バリデーションエラー:`BindingResult`でフォーム上にフィールドごと表示
- システムエラー(500など):共通の`error.html`にリダイレクト
- 雛形はAかBが最初に用意する

**(7) 結合のタイミング**
Gitフローは `feature/xxx` → `develop`。**毎週決まった曜日に全員でdevelopにマージして動作確認する会を、開発初日から予定として組み込む。** 終盤にまとめて結合するのは避ける。

## 8. 特に注意すべき点

**(8-1) なぜ画面単位ではなく機能ドメイン単位で分担するか**
Thymeleafはコントローラーが詰めたデータをテンプレートが直接埋め込む方式のため、画面と機能が密接に結合している。画面担当と機能担当を分けると、画面ごとに「Modelに詰めるデータの形」をすり合わせる手間が画面数分発生し、バリデーションや権限判定のような機能寄りの処理がテンプレート側にも入り込むため、どちらが直すべきか曖昧なバグが増えやすい。機能ドメイン単位なら、この種の取り決めは各自の中で完結する。

- 招待コードで参加した人が`pending`状態のとき、承認前に予定を見せない画面制御を忘れないこと
- 一括登録で1件でも保存に失敗した場合はトランザクションで全件ロールバックする
- パスワードは`BCryptPasswordEncoder`でハッシュ化。ログイン失敗時のエラーメッセージは「メールアドレスまたはパスワードが違います」で統一し、メール存在の有無を教えない
- `schedules`の複合インデックス`(calendar_id, start_time, end_time)`は月表示・空き日程の両方で使われる最重要インデックスなので、DDLから削らないこと
- メールOTP送信は学校ネットワークでポートがブロックされる可能性があるため、開発初日に疎通テストを済ませておくこと(GitHubプッシュで起きたのと同種のリスク)

## 9. 機能フロー

### 9-1. 認証フロー(2段階認証あり)

1. ログイン画面でメールアドレス・パスワードを入力
2. サーバー側でパスワード照合。**この時点ではまだセッションを発行しない**
3. 6桁コードを生成して`otp_codes`に保存し、メール送信
4. 「認証コード入力」画面でコードを入力
5. `otp_codes`と照合(有効期限内・未使用・試行回数以内)が一致したらセッションを発行し、カレンダー一覧へ遷移

### 9-2. カレンダー作成・招待フロー

1. カレンダー一覧の「作成」からカレンダー名を入力
2. `calendars`に1件作成、作成者を`calendar_members`に`role=owner, status=accepted`で登録
3. 招待コードを発行(`invite_codes`)、期限内であれば誰でも入力してメンバーになれる
4. 参加希望者がコード入力 → `calendar_members`に`status=pending`(または即`accepted`)で追加

### 9-3. 予定登録フロー(2パターン)

**パターンA:直接登録(単一 / 一括とも)**
1. 日程(1件、または複数件それぞれ別タイトルで)を入力して保存
2. `schedules`に確定済みとして1行(複数件なら複数行)作成 → カレンダーに即反映、メンバーに通知

**パターンB:候補調整(調整さん型)**
1. 幹事が「一括予定登録」で候補日程を複数入力
2. `schedule_polls`(`status=open`)と`poll_candidates`(候補の数だけ)を作成
3. メンバーが「空き日程を探す」画面で各候補に○×回答 → `poll_responses`に記録
4. 候補ごとの参加人数を集計して表示
5. 幹事が候補を1つ選んで確定 → その日時で`schedules`に1件作成、`schedule_polls.confirmed_schedule_id`をセットし`status=confirmed`に変更
6. 確定後は通常の予定と同じ扱い(コメント・通知はここから`schedules`に紐づく)

### 9-4. コメント・通知フロー

1. 確定済みの予定(`schedules`)に対してコメント投稿 → `comments`に保存
2. 投稿・招待申請・予定確定などのタイミングで`notifications`にレコードを作成し、通知一覧に表示

### 9-5. リマインドフロー(バッチ処理)

1. 毎日決まった時刻に、翌日開始の`schedules`を持つメンバー全員分の通知を`notifications`に作成する定期処理(`@Scheduled`)を実行
2. DBの構造変更は不要だが、**この定期処理自体の実装が新規タスクとして発生する**(担当は空き日程ロジックと親和性が高いDが候補)
